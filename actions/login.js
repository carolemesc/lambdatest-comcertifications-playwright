import { expect } from '@playwright/test'

/** @param {import('@playwright/test').Page} page */
const Approval = (page) => {
  async function createGrant() {
  }  

  return {}
}

const approvalFactory = (page) => Approval(page)
export default approvalFactory